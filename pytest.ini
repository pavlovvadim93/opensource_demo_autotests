[pytest]
addopts =
    -v
    --strict-markers
    --html=reports/report.html
    --self-contained-html
    --alluredir=allure-results
markers =
    smoke: Smoke tests
    regression: Regression tests
    api: API tests
testpaths = tests